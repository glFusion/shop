{# begin {templatelocation} #}
        <!-- BEGIN ItemOptions -->
    <br /><span class="ItemOptions">
        --&nbsp;{opt_name}:&nbsp;{opt_value}
{!if 0}
          {!if opt_price}
            &nbsp;({opt_price})
          {!endif}
{!endif}
    </span>
        <!-- END ItemOptions -->
{# end {templatelocation} #}
