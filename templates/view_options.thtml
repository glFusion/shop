{# begin {templatelocation} #}
        <!-- BEGIN ItemOptions -->
    <span class="ItemOptions">
        --&nbsp;{opt_name}:&nbsp;{opt_value}
{!if 0}
          {!if opt_price}
            &nbsp;({opt_price})
          {!endif}
{!endif}
    </span>
        <br />
        <!-- END ItemOptions -->
{# end {templatelocation} #}
