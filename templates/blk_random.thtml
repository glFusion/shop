{# begin {templatelocation} #}
{!if rec_num}
  <hr />
{!endif}
<a href="{item_url}">
{!if img_url}
<div class="blk_img">
  <img src="{img_url}" />
</div>
{!endif}
</a>
<p>{descrip}</p>
{!if price}
<p>{$LANG_SHOP['price']}: {price} {currency}</p>
{!endif}
<p>{button}</p>
{# end {templatelocation} #}
