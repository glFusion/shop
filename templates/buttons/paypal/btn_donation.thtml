{# begin {templatelocation} #}
<form style="display:inline;" class="frm_donation" action="{action_url}" method="{method}">
  {gateway_vars}
  <button type="submit" class="uk-button uk-button-small uk-button-primary">
    <i class="uk-icon uk-icon-paypal"></i>&nbsp;{$LANG_SHOP['donate']}</button>
</form>
{# end {templatelocation} #}
