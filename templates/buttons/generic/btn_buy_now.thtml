{# begin {templatelocation} #}
<form style="display:inline;" class="frm_buy_now" 
    action="{action_url}" method="{method}">
  {gateway_vars}
  <input type="hidden" name="gateway" value="{gw_name}" />
  <input type="submit" class="shopButton green" name="processorder" value="{btn_text}" />
</form>
{# end {templatelocation} #}
