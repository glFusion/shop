{# begin {templatelocation} #}
<form class="frm_buy_now" action="{action_url}" method="{method}">
  {gateway_vars}
  <input type="hidden" name="gateway" value="{gw_name}" />
  <button type="submit" class="shopButton green" name="processorder" value="{btn_text}">{btn_text}</button>
</form>
{# end {templatelocation} #}
